import { CircleLinkDirective } from './circle-link.directive';

describe('CircleLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CircleLinkDirective();
    expect(directive).toBeTruthy();
  });
});
